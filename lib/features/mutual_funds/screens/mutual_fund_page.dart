import 'package:flutter/material.dart';

class MutualFundScreen extends StatelessWidget {
  const MutualFundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('MutualFundScreen'),
    );
  }
}
