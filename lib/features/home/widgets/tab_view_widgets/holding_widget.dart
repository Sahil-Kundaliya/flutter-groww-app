import 'package:flutter/material.dart';

class HoldingWidget extends StatelessWidget {
  const HoldingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Holding Widget');
  }
}
